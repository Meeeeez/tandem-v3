---
import "./Process.scss";
import Header from "../../components/Header/Header.astro";
import FullscreenSlider from "../../components/FullscreenSlider/FullscreenSlider.astro";
import { getLangFromUrl, useTranslations } from "../../i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const slideImages = [
  "/tandem-v3/img/tandem-paragliding-above-bressanone.avif",
  "/tandem-v3/img/tandem-paragliding-meetup.avif",
  "/tandem-v3/img/tandem-paragliding-launch-odles.avif",
  "/tandem-v3/img/tandem-paragliding-sunset.avif",
  "/tandem-v3/img/tandem-paragliding-plose.avif",
];

const slides = t.process.slides.map((slide, i) => ({
  title: slide.title,
  description: slide.description,
  imageAlt: slide.imageAlt,
  imageSrc: slideImages[i],
}));
---

<section class="Process" id="process">
  <FullscreenSlider slides={slides}>
    <Header
      slot="header"
      title={t.process.header.title}
      titleHighlighted={t.process.header.titleHighlighted}
      description={t.process.header.description}
      onPrimaryBackground
    />
  </FullscreenSlider>
</section>
